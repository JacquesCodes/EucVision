# Ensure the destination folder for the master CSV exists before saving
master_csv_folder <- dirname(dest_master_csv)
if (!dir.exists(master_csv_folder)) {
dir.create(master_csv_folder, recursive = TRUE)
}
# Write over the previous master CSV
write_csv(master_dataset, dest_master_csv)
cat(paste("Master dataset created successfully with", nrow(master_dataset), "rows.\n"))
cat("Process complete!\n")
} else {
cat("No CSV files were found to merge.\n")
}
# =====================================================================
# 1. DEFINE DIRECTORIES
# =====================================================================
# Base directory where all the UAV datasets are stored
src_base_dir <- "E:/Remote Sensing Media"# Load necessary libraries
# Install with install.packages(c("dplyr", "readr", "stringr")) if needed
library(dplyr)
library(readr)
library(stringr)
# =====================================================================
# 1. DEFINE DIRECTORIES
# =====================================================================
src_base_dir <- "E:/Remote Sensing Media"
dest_backup_dir <- "C:/Users/jakev/Stellenbosch University/JacquesV B.Sc. skripsie M.Sc. project - Documents/Processed Data/EucVision/05. Crown Metrics"
dest_master_csv <- "C:/Users/jakev/Stellenbosch University/JacquesV B.Sc. skripsie M.Sc. project - Documents/Processed Data/EucVision/01. Data analysis/01. Main dataset CSV.csv"
# =====================================================================
# 2. IDENTIFY FOLDERS & INITIALIZE LIST
# =====================================================================
main_folders <- list.dirs(src_base_dir, recursive = FALSE)
csv_list <- list()
cat("Starting Teams backup and data merge...\n")
# =====================================================================
# 3. LOOP THROUGH FOLDERS, COPY FILES, AND CLEAN CSVS
# =====================================================================
for (folder in main_folders) {
folder_name <- basename(folder)
# Extract the date from the folder name (e.g., "25 February 2025")
# and format it to match the template (e.g., "25-Feb-25")
date_match <- str_extract(folder_name, "\\d{2} [A-Za-z]+ \\d{4}")
formatted_date <- NA
if (!is.na(date_match)) {
parsed_date <- as.Date(date_match, format="%d %B %Y")
formatted_date <- format(parsed_date, "%d-%b-%y")
}
crown_metrics_path <- file.path(folder, "09. Crown Metrics")
if (dir.exists(crown_metrics_path)) {
files_to_copy <- list.files(crown_metrics_path,
pattern = "\\.(shp|shx|dbf|prj|csv)$",
full.names = TRUE,
ignore.case = TRUE)
if (length(files_to_copy) > 0) {
current_dest_dir <- file.path(dest_backup_dir, folder_name)
if (!dir.exists(current_dest_dir)) {
dir.create(current_dest_dir, recursive = TRUE)
}
file.copy(from = files_to_copy, to = current_dest_dir, overwrite = TRUE)
cat(paste("Backed up files for:", folder_name, "\n"))
csv_file <- files_to_copy[grepl("\\.csv$", files_to_copy, ignore.case = TRUE)]
if (length(csv_file) == 1) {
temp_df <- read_csv(csv_file, show_col_types = FALSE)
# --- DATA CLEANING TO MATCH TEMPLATE ---
# 1. Standardize Compartment name
if ("Cmprtmn" %in% names(temp_df)) {
temp_df <- rename(temp_df, Compartment = Cmprtmn)
}
# 2. Standardize Area names to 'Crown_Area'
if ("Area_m2" %in% names(temp_df) && !"Area" %in% names(temp_df)) {
temp_df <- rename(temp_df, Crown_Area = Area_m2)
} else if ("Area" %in% names(temp_df) && !"Area_m2" %in% names(temp_df)) {
temp_df <- rename(temp_df, Crown_Area = Area)
} else if ("Area_m2" %in% names(temp_df) && "Area" %in% names(temp_df)) {
temp_df <- mutate(temp_df, Crown_Area = coalesce(Area_m2, Area))
}
# 3. Add the properly formatted Date
temp_df$Date <- formatted_date
# 4. Filter strictly for Template columns to drop junk columns (grop_ld, Plt_shp)
cols_to_keep <- c("Compartment", "Line", "Plot", "Culture", "Spacing",
"Species", "Tree", "Date", "Crown_Area", "Tree_Height")
temp_df <- select(temp_df, any_of(cols_to_keep))
csv_list[[folder_name]] <- temp_df
}
}
}
}
# =====================================================================
# 4. MERGE CSVs AND EXPORT MASTER DATASET
# =====================================================================
if (length(csv_list) > 0) {
cat("Merging CSV files...\n")
master_dataset <- bind_rows(csv_list)
master_csv_folder <- dirname(dest_master_csv)
if (!dir.exists(master_csv_folder)) {
dir.create(master_csv_folder, recursive = TRUE)
}
write_csv(master_dataset, dest_master_csv)
cat(paste("Master dataset perfectly formatted and updated with", nrow(master_dataset), "rows.\n"))
} else {
cat("No CSV files were found to merge.\n")
}
# Teams Backup Directory (This my Teams backup)
dest_backup_dir <- "C:/Users/jakev/Stellenbosch University/JacquesV B.Sc. skripsie M.Sc. project - Documents/Processed Data/EucVision/05. Crown Metrics"
# Master CSV Destination Directory (This my Masteer CSV file)
dest_master_csv <- "C:/Users/jakev/Stellenbosch University/JacquesV B.Sc. skripsie M.Sc. project - Documents/Processed Data/EucVision/01. Data analysis/01. Main dataset CSV.csv"
# =====================================================================
# 2. IDENTIFY FOLDERS & FILES
# =====================================================================
# Get all main folders in the E: drive
main_folders <- list.dirs(src_base_dir, recursive = FALSE)
# Initialize an empty list to store the CSV data for merging
csv_list <- list()
cat("Starting Teams backup and data merge...\n")
# =====================================================================
# 3. LOOP THROUGH FOLDERS, COPY FILES, AND READ CSVS
# =====================================================================
for (folder in main_folders) {
folder_name <- basename(folder)
# Path to the specific Crown Metrics subfolder
crown_metrics_path <- file.path(folder, "09. Crown Metrics")
# Check if the "09. Crown Metrics" folder actually exists in this main directory
if (dir.exists(crown_metrics_path)) {
# Locate the shapefile components and the CSV
# Using a regular expression to grab .shp, .shx, .dbf, .prj, and .csv
files_to_copy <- list.files(crown_metrics_path,
pattern = "\\.(shp|shx|dbf|prj|csv)$",
full.names = TRUE,
ignore.case = TRUE)
# Only proceed if we actually found files
if (length(files_to_copy) > 0) {
# Define the new destination folder for this specific date
current_dest_dir <- file.path(dest_backup_dir, folder_name)
# Create the destination folder if it doesn't exist
if (!dir.exists(current_dest_dir)) {
dir.create(current_dest_dir, recursive = TRUE)
}
# Copy files to the Teams backup (overwrite = TRUE ensures updates replace old files)
file.copy(from = files_to_copy, to = current_dest_dir, overwrite = TRUE)
cat(paste("Successfully backed up files for:", folder_name, "\n"))
# Identify the CSV file from the copied list
csv_file <- files_to_copy[grepl("\\.csv$", files_to_copy, ignore.case = TRUE)]
if (length(csv_file) == 1) {
# Read the CSV and add it to our list
# Suppressing column type messages for a cleaner console output
temp_df <- read_csv(csv_file, show_col_types = FALSE)
# Optional: Add a column to track which date/folder the data came from
# temp_df$Source_Date <- folder_name
csv_list[[folder_name]] <- temp_df
}
}
}
}
# =====================================================================
# 4. MERGE CSVs AND EXPORT MASTER DATASET
# =====================================================================
# Check if we successfully gathered any CSVs
if (length(csv_list) > 0) {
cat("Merging CSV files...\n")
# bind_rows will stack all the dataframes together, matching them by column headings
master_dataset <- bind_rows(csv_list)
# Ensure the destination folder for the master CSV exists before saving
master_csv_folder <- dirname(dest_master_csv)
if (!dir.exists(master_csv_folder)) {
dir.create(master_csv_folder, recursive = TRUE)
}
# Write over the previous master CSV
write_csv(master_dataset, dest_master_csv)
cat(paste("Master dataset created successfully with", nrow(master_dataset), "rows.\n"))
cat("Process complete!\n")
} else {
cat("No CSV files were found to merge.\n")
}
install.packages(c("dplyr", "readr", "stringr")) if needed
install.packages(c("dplyr", "readr", "stringr"))
install.packages(c("dplyr", "readr", "stringr"))
install.packages(c("dplyr", "readr", "stringr"))
install.packages(c("dplyr", "readr", "stringr"))
# =====================================================================
# 1. DEFINE DIRECTORIES
# =====================================================================
# Base directory where all the UAV datasets are stored
src_base_dir <- "E:/Remote Sensing Media"# Load necessary libraries
# Install with install.packages(c("dplyr", "readr", "stringr")) if needed
library(dplyr)
library(readr)
library(stringr)
# =====================================================================
# 1. DEFINE DIRECTORIES
# =====================================================================
src_base_dir <- "E:/Remote Sensing Media"
dest_backup_dir <- "C:/Users/jakev/Stellenbosch University/JacquesV B.Sc. skripsie M.Sc. project - Documents/Processed Data/EucVision/05. Crown Metrics"
dest_master_csv <- "C:/Users/jakev/Stellenbosch University/JacquesV B.Sc. skripsie M.Sc. project - Documents/Processed Data/EucVision/01. Data analysis/01. Main dataset CSV.csv"
# =====================================================================
# 2. IDENTIFY FOLDERS & INITIALIZE LIST
# =====================================================================
main_folders <- list.dirs(src_base_dir, recursive = FALSE)
csv_list <- list()
cat("Starting Teams backup and data merge...\n")
# =====================================================================
# 3. LOOP THROUGH FOLDERS, COPY FILES, AND CLEAN CSVS
# =====================================================================
for (folder in main_folders) {
folder_name <- basename(folder)
# Extract the date from the folder name (e.g., "25 February 2025")
# and format it to match the template (e.g., "25-Feb-25")
date_match <- str_extract(folder_name, "\\d{2} [A-Za-z]+ \\d{4}")
formatted_date <- NA
if (!is.na(date_match)) {
parsed_date <- as.Date(date_match, format="%d %B %Y")
formatted_date <- format(parsed_date, "%d-%b-%y")
}
crown_metrics_path <- file.path(folder, "09. Crown Metrics")
if (dir.exists(crown_metrics_path)) {
files_to_copy <- list.files(crown_metrics_path,
pattern = "\\.(shp|shx|dbf|prj|csv)$",
full.names = TRUE,
ignore.case = TRUE)
if (length(files_to_copy) > 0) {
current_dest_dir <- file.path(dest_backup_dir, folder_name)
if (!dir.exists(current_dest_dir)) {
dir.create(current_dest_dir, recursive = TRUE)
}
file.copy(from = files_to_copy, to = current_dest_dir, overwrite = TRUE)
cat(paste("Backed up files for:", folder_name, "\n"))
csv_file <- files_to_copy[grepl("\\.csv$", files_to_copy, ignore.case = TRUE)]
if (length(csv_file) == 1) {
temp_df <- read_csv(csv_file, show_col_types = FALSE)
# --- DATA CLEANING TO MATCH TEMPLATE ---
# 1. Standardize Compartment name
if ("Cmprtmn" %in% names(temp_df)) {
temp_df <- rename(temp_df, Compartment = Cmprtmn)
}
# 2. Standardize Area names to 'Crown_Area'
if ("Area_m2" %in% names(temp_df) && !"Area" %in% names(temp_df)) {
temp_df <- rename(temp_df, Crown_Area = Area_m2)
} else if ("Area" %in% names(temp_df) && !"Area_m2" %in% names(temp_df)) {
temp_df <- rename(temp_df, Crown_Area = Area)
} else if ("Area_m2" %in% names(temp_df) && "Area" %in% names(temp_df)) {
temp_df <- mutate(temp_df, Crown_Area = coalesce(Area_m2, Area))
}
# 3. Add the properly formatted Date
temp_df$Date <- formatted_date
# 4. Filter strictly for Template columns to drop junk columns (grop_ld, Plt_shp)
cols_to_keep <- c("Compartment", "Line", "Plot", "Culture", "Spacing",
"Species", "Tree", "Date", "Crown_Area", "Tree_Height")
temp_df <- select(temp_df, any_of(cols_to_keep))
csv_list[[folder_name]] <- temp_df
}
}
}
}
# =====================================================================
# 4. MERGE CSVs AND EXPORT MASTER DATASET
# =====================================================================
if (length(csv_list) > 0) {
cat("Merging CSV files...\n")
master_dataset <- bind_rows(csv_list)
master_csv_folder <- dirname(dest_master_csv)
if (!dir.exists(master_csv_folder)) {
dir.create(master_csv_folder, recursive = TRUE)
}
write_csv(master_dataset, dest_master_csv)
cat(paste("Master dataset perfectly formatted and updated with", nrow(master_dataset), "rows.\n"))
} else {
cat("No CSV files were found to merge.\n")
}
# =====================================================================
# 1. DEFINE DIRECTORIES
# =====================================================================
# Base directory where all the UAV datasets are stored
src_base_dir <- "E:/Remote Sensing Media"# Load necessary libraries
# Install with install.packages(c("dplyr", "readr", "stringr")) if needed
library(dplyr)
library(readr)
library(stringr)
# =====================================================================
# 1. DEFINE DIRECTORIES
# =====================================================================
src_base_dir <- "E:/Remote Sensing Media"
dest_backup_dir <- "C:/Users/jakev/Stellenbosch University/JacquesV B.Sc. skripsie M.Sc. project - Documents/Processed Data/EucVision/05. Crown Metrics"
dest_master_csv <- "C:/Users/jakev/Stellenbosch University/JacquesV B.Sc. skripsie M.Sc. project - Documents/Processed Data/EucVision/01. Data analysis/01. Main dataset CSV.csv"
# =====================================================================
# 2. IDENTIFY FOLDERS & INITIALIZE LIST
# =====================================================================
main_folders <- list.dirs(src_base_dir, recursive = FALSE)
csv_list <- list()
cat("Starting Teams backup and data merge...\n")
# =====================================================================
# 3. LOOP THROUGH FOLDERS, COPY FILES, AND CLEAN CSVS
# =====================================================================
for (folder in main_folders) {
folder_name <- basename(folder)
# Extract the date from the folder name (e.g., "25 February 2025")
# and format it to match the template (e.g., "25-Feb-25")
date_match <- str_extract(folder_name, "\\d{2} [A-Za-z]+ \\d{4}")
formatted_date <- NA
if (!is.na(date_match)) {
parsed_date <- as.Date(date_match, format="%d %B %Y")
formatted_date <- format(parsed_date, "%d-%b-%y")
}
crown_metrics_path <- file.path(folder, "09. Crown Metrics")
if (dir.exists(crown_metrics_path)) {
files_to_copy <- list.files(crown_metrics_path,
pattern = "\\.(shp|shx|dbf|prj|csv)$",
full.names = TRUE,
ignore.case = TRUE)
if (length(files_to_copy) > 0) {
current_dest_dir <- file.path(dest_backup_dir, folder_name)
if (!dir.exists(current_dest_dir)) {
dir.create(current_dest_dir, recursive = TRUE)
}
file.copy(from = files_to_copy, to = current_dest_dir, overwrite = TRUE)
cat(paste("Backed up files for:", folder_name, "\n"))
csv_file <- files_to_copy[grepl("\\.csv$", files_to_copy, ignore.case = TRUE)]
if (length(csv_file) == 1) {
temp_df <- read_csv(csv_file, show_col_types = FALSE)
# --- DATA CLEANING TO MATCH TEMPLATE ---
# 1. Standardize Compartment name
if ("Cmprtmn" %in% names(temp_df)) {
temp_df <- rename(temp_df, Compartment = Cmprtmn)
}
# 2. Standardize Area names to 'Crown_Area'
if ("Area_m2" %in% names(temp_df) && !"Area" %in% names(temp_df)) {
temp_df <- rename(temp_df, Crown_Area = Area_m2)
} else if ("Area" %in% names(temp_df) && !"Area_m2" %in% names(temp_df)) {
temp_df <- rename(temp_df, Crown_Area = Area)
} else if ("Area_m2" %in% names(temp_df) && "Area" %in% names(temp_df)) {
temp_df <- mutate(temp_df, Crown_Area = coalesce(Area_m2, Area))
}
# 3. Add the properly formatted Date
temp_df$Date <- formatted_date
# 4. Filter strictly for Template columns to drop junk columns (grop_ld, Plt_shp)
cols_to_keep <- c("Compartment", "Line", "Plot", "Culture", "Spacing",
"Species", "Tree", "Date", "Crown_Area", "Tree_Height")
temp_df <- select(temp_df, any_of(cols_to_keep))
csv_list[[folder_name]] <- temp_df
}
}
}
}
# =====================================================================
# 4. MERGE CSVs AND EXPORT MASTER DATASET
# =====================================================================
if (length(csv_list) > 0) {
cat("Merging CSV files...\n")
master_dataset <- bind_rows(csv_list)
master_csv_folder <- dirname(dest_master_csv)
if (!dir.exists(master_csv_folder)) {
dir.create(master_csv_folder, recursive = TRUE)
}
write_csv(master_dataset, dest_master_csv)
cat(paste("Master dataset perfectly formatted and updated with", nrow(master_dataset), "rows.\n"))
} else {
cat("No CSV files were found to merge.\n")
}
# Teams Backup Directory (This my Teams backup)
dest_backup_dir <- "C:/Users/jakev/Stellenbosch University/JacquesV B.Sc. skripsie M.Sc. project - Documents/Processed Data/EucVision/05. Crown Metrics"
# Master CSV Destination Directory (This my Masteer CSV file)
dest_master_csv <- "C:/Users/jakev/Stellenbosch University/JacquesV B.Sc. skripsie M.Sc. project - Documents/Processed Data/EucVision/01. Data analysis/01. Main dataset CSV.csv"
# =====================================================================
# 2. IDENTIFY FOLDERS & FILES
# =====================================================================
# Get all main folders in the E: drive
main_folders <- list.dirs(src_base_dir, recursive = FALSE)
# Initialize an empty list to store the CSV data for merging
csv_list <- list()
cat("Starting Teams backup and data merge...\n")
# =====================================================================
# 3. LOOP THROUGH FOLDERS, COPY FILES, AND READ CSVS
# =====================================================================
for (folder in main_folders) {
folder_name <- basename(folder)
# Path to the specific Crown Metrics subfolder
crown_metrics_path <- file.path(folder, "09. Crown Metrics")
# Check if the "09. Crown Metrics" folder actually exists in this main directory
if (dir.exists(crown_metrics_path)) {
# Locate the shapefile components and the CSV
# Using a regular expression to grab .shp, .shx, .dbf, .prj, and .csv
files_to_copy <- list.files(crown_metrics_path,
pattern = "\\.(shp|shx|dbf|prj|csv)$",
full.names = TRUE,
ignore.case = TRUE)
# Only proceed if we actually found files
if (length(files_to_copy) > 0) {
# Define the new destination folder for this specific date
current_dest_dir <- file.path(dest_backup_dir, folder_name)
# Create the destination folder if it doesn't exist
if (!dir.exists(current_dest_dir)) {
dir.create(current_dest_dir, recursive = TRUE)
}
# Copy files to the Teams backup (overwrite = TRUE ensures updates replace old files)
file.copy(from = files_to_copy, to = current_dest_dir, overwrite = TRUE)
cat(paste("Successfully backed up files for:", folder_name, "\n"))
# Identify the CSV file from the copied list
csv_file <- files_to_copy[grepl("\\.csv$", files_to_copy, ignore.case = TRUE)]
if (length(csv_file) == 1) {
# Read the CSV and add it to our list
# Suppressing column type messages for a cleaner console output
temp_df <- read_csv(csv_file, show_col_types = FALSE)
# Optional: Add a column to track which date/folder the data came from
# temp_df$Source_Date <- folder_name
csv_list[[folder_name]] <- temp_df
}
}
}
}
# =====================================================================
# 4. MERGE CSVs AND EXPORT MASTER DATASET
# =====================================================================
# Check if we successfully gathered any CSVs
if (length(csv_list) > 0) {
cat("Merging CSV files...\n")
# bind_rows will stack all the dataframes together, matching them by column headings
master_dataset <- bind_rows(csv_list)
# Ensure the destination folder for the master CSV exists before saving
master_csv_folder <- dirname(dest_master_csv)
if (!dir.exists(master_csv_folder)) {
dir.create(master_csv_folder, recursive = TRUE)
}
# Write over the previous master CSV
write_csv(master_dataset, dest_master_csv)
cat(paste("Master dataset created successfully with", nrow(master_dataset), "rows.\n"))
cat("Process complete!\n")
} else {
cat("No CSV files were found to merge.\n")
}
# 1. Install missing packages if necessary
if (!require("dplyr")) install.packages("dplyr")
if (!require("readr")) install.packages("readr")
if (!require("stringr")) install.packages("stringr")
library(dplyr)
library(readr)
library(stringr)
# Force R to use English for date parsing (fixes South African locale issues)
Sys.setlocale("LC_TIME", "C")
# =====================================================================
# 1. DEFINE DIRECTORIES
# =====================================================================
src_base_dir <- "E:/Remote Sensing Media"
dest_backup_dir <- "C:/Users/jakev/Stellenbosch University/JacquesV B.Sc. skripsie M.Sc. project - Documents/Processed Data/EucVision/05. Crown Metrics"
dest_master_csv <- "C:/Users/jakev/Stellenbosch University/JacquesV B.Sc. skripsie M.Sc. project - Documents/Processed Data/EucVision/01. Data analysis/01. Main dataset CSV.csv"
# =====================================================================
# 2. RUN SCRIPT
# =====================================================================
main_folders <- list.dirs(src_base_dir, recursive = FALSE)
csv_list <- list()
cat("\n--- Starting Teams backup and data merge ---\n")
for (folder in main_folders) {
folder_name <- basename(folder)
# Extract the date from the folder name
date_match <- str_extract(folder_name, "\\d{2} [A-Za-z]+ \\d{4}")
formatted_date <- NA
if (!is.na(date_match)) {
parsed_date <- as.Date(date_match, format="%d %B %Y")
formatted_date <- format(parsed_date, "%d-%b-%y")
}
crown_metrics_path <- file.path(folder, "09. Crown Metrics")
if (dir.exists(crown_metrics_path)) {
files_to_copy <- list.files(crown_metrics_path,
pattern = "\\.(shp|shx|dbf|prj|csv)$",
full.names = TRUE,
ignore.case = TRUE)
if (length(files_to_copy) > 0) {
current_dest_dir <- file.path(dest_backup_dir, folder_name)
if (!dir.exists(current_dest_dir)) dir.create(current_dest_dir, recursive = TRUE)
file.copy(from = files_to_copy, to = current_dest_dir, overwrite = TRUE)
csv_file <- files_to_copy[grepl("\\.csv$", files_to_copy, ignore.case = TRUE)]
if (length(csv_file) == 1) {
temp_df <- read_csv(csv_file, show_col_types = FALSE)
# Clean Compartment
if ("Cmprtmn" %in% names(temp_df)) temp_df <- rename(temp_df, Compartment = Cmprtmn)
# Clean Area
if ("Area_m2" %in% names(temp_df) && !"Area" %in% names(temp_df)) {
temp_df <- rename(temp_df, Crown_Area = Area_m2)
} else if ("Area" %in% names(temp_df) && !"Area_m2" %in% names(temp_df)) {
temp_df <- rename(temp_df, Crown_Area = Area)
} else if ("Area_m2" %in% names(temp_df) && "Area" %in% names(temp_df)) {
temp_df <- mutate(temp_df, Crown_Area = coalesce(Area_m2, Area))
}
# Apply Date
temp_df$Date <- formatted_date
# Filter Columns strictly matching your Template
cols_to_keep <- c("Compartment", "Line", "Plot", "Culture", "Spacing",
"Species", "Tree", "Date", "Crown_Area", "Tree_Height")
temp_df <- select(temp_df, any_of(cols_to_keep))
csv_list[[folder_name]] <- temp_df
cat(paste("SUCCESS: Processed ->", folder_name, "\n"))
}
}
}
}
# =====================================================================
# 3. EXPORT MASTER DATASET
# =====================================================================
if (length(csv_list) > 0) {
cat("\nMerging CSV files...\n")
master_dataset <- bind_rows(csv_list)
master_csv_folder <- dirname(dest_master_csv)
if (!dir.exists(master_csv_folder)) dir.create(master_csv_folder, recursive = TRUE)
# Try Catch block to check if the file is locked by Excel
tryCatch({
write_csv(master_dataset, dest_master_csv)
cat(paste("\nDONE: Master dataset updated with", nrow(master_dataset), "rows.\n"))
cat("Please check the '01. Main dataset CSV.csv' file now!\n")
}, error = function(e) {
cat("\nERROR: Could not overwrite the master CSV.\n")
cat("Is '01. Main dataset CSV.csv' currently open in Excel? If yes, please close it and run again.\n")
cat("Technical error message:", conditionMessage(e), "\n")
})
} else {
cat("\nNo CSV files were found in the E: drive to merge.\n")
}
